# Compliance Academy - Python dependencies

# Azure OpenAI client (used by the Scenario Generator and other agent wrappers)
openai>=1.40.0

# Entra ID auth via DefaultAzureCredential (picks up az login)
azure-identity>=1.15.0

# Azure AI Search client for Foundry IQ retrieval grounding
# (used by Forensic Analyst and Compliance Officer to pull policy and
# framework context from the compliance-content-index)
azure-search-documents>=11.5.0

# Load .env files at module load so CLI smoke tests pick up Azure config
python-dotenv>=1.0.0

# -- UI dependencies (the Chainlit demo surface) --

# Chainlit: the chat-style UI that the audience sees on stream.
# Runs as `chainlit run app.py`. The CLI orchestrator and unit tests
# do not depend on this; install is required only for the UI path.
chainlit>=1.3.0

# Pillow: used by scripts/build_avatars.py to generate the 9 agent
# avatar PNGs (initials in colored circles). One-time generation; the
# resulting PNGs live in .chainlit/public/avatars/.
pillow>=10.0.0

# -- Test dependencies (pytest discovers tests/ automatically) --

# pytest: test runner and assertion engine
pytest>=8.0.0

# pytest-mock: convenient mocker fixture for unit tests that need to stub
# out Azure calls without hitting the network
pytest-mock>=3.12.0
